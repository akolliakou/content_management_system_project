<% if session[:message] %>
    <p><%= session.delete(:message) %></p>
<% end %>

<ul>
  <% @files.sort.each do |file| %>
    <li>
      <a href="/<%= file %>"><%= file %></a> 
      <a href="/<%= file %>/edit">edit</a>
    </li>
  <% end %>
</ul>

